---
## run: ansible-playbook 0_cluster_virtual.yaml -i hosts.ini --ask-vault-pass  [1ai_cloud_init_vars.yaml is encrypted]
# provision cluster hosts with multipass
- import_playbook: 1_multipass.yaml
# apt install non-microceph & non-microk8s packages
- import_playbook: 2_postgresql_client_kubernetes.yaml
# install, bootstrap, assemble and provision microceph cluster
- import_playbook: 3_microceph.yaml
# install, assemble, configure, and deploy postgres and redis
- import_playbook: 4_microk8s.yaml
# deploy containers for microservices, apiserver and webdisplay
#- import_playbook: 5_application_containers.yaml
